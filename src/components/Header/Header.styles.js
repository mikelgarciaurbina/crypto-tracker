import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  header: {
    fontSize: 20,
    fontWeight: 'bold',
  },
  headerContainer: {
    alignItems: 'center',
    display: 'flex',
    marginTop: 55,
  },
});
